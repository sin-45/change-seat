[package]
name = "main"
version = "0.1.0"
edition = "2024"

[dependencies]
csv = "1.3"      # CSVの読み書き
rand = "0.8"     # 乱数生成とシャッフル
rand_pcg = "0.3" # シード可能な乱数生成器 (Pythonのrandom.seed相当)
sha2 = "0.10"    # SHA-256 ハッシュ
hex = "0.4"      # ハッシュ値の16進文字列変換